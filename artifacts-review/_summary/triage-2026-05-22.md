# FSI-CopilotGov-Solutions — Council Review Triage Report

**Source:** AI Council Review 2026-05-22 (23 solutions, 323 findings)
**Process:** Each finding independently re-verified against actual code per user mandate ('researcher might be wrong; re-verify before updating repo').
**Methodology:** 23 `general-purpose` sub-agents (Sonnet) inspected real files in `C:\Dev\FSI-CopilotGov-Solutions`. High/Critical verdicts further challenged by an independent `rubber-duck` agent (all 11 ratified, zero overturns).

## Executive Summary

- **Total findings:** 323
- **Verified actionable:** 119 (36%) — these are real issues we will fix.
- **Debatable:** 86 (26%) — require user input before action.
- **No action required:** 118 (36%) — design-intent, reviewer-wrong, or duplicate.

- **Reviewer accuracy:** 39 findings (12%) were classified REJECTED-WRONG. The reviewer's signal is high but not perfect — every actionable verdict was empirically verified before classification.

## Classification breakdown

| Classification | Total | CRITICAL | HIGH | MEDIUM | LOW | INFO | Action |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| VERIFIED-BUG | 61 | 0 | 7 | 24 | 24 | 6 | **Fix in PR** |
| VERIFIED-DEAD-CONFIG | 26 | 0 | 2 | 5 | 18 | 1 | **Fix in PR** |
| VERIFIED-DOC-OVERCLAIM | 25 | 1 | 1 | 9 | 12 | 2 | **Fix in PR** |
| VERIFIED-VERSION-DRIFT | 7 | 0 | 0 | 3 | 3 | 1 | **Fix in PR** |
| DEBATABLE | 86 | 0 | 4 | 15 | 49 | 18 | **User input needed** |
| DESIGN-INTENT | 66 | 0 | 4 | 9 | 25 | 28 | No action |
| REJECTED-WRONG | 39 | 0 | 2 | 12 | 17 | 8 | No action |
| DUPLICATE | 13 | 0 | 1 | 7 | 1 | 4 | No action |

## Per-solution summary

| # | Solution | Findings | Verified actionable | Debatable | No action |
| ---: | --- | ---: | ---: | ---: | ---: |
| 01 | `01-copilot-readiness-scanner` | 10 | 1 | 2 | 7 |
| 02 | `02-oversharing-risk-assessment` | 7 | 0 | 4 | 3 |
| 03 | `03-sensitivity-label-auditor` | 14 | 4 | 4 | 6 |
| 04 | `04-finra-supervision-workflow` | 14 | 6 | 4 | 4 |
| 05 | `05-dlp-policy-governance` | 7 | 1 | 5 | 1 |
| 06 | `06-audit-trail-manager` | 12 | 2 | 5 | 5 |
| 07 | `07-conditional-access-automation` | 9 | 4 | 3 | 2 |
| 08 | `08-license-governance-roi` | 12 | 0 | 4 | 8 |
| 09 | `09-feature-management-controller` | 23 | 7 | 6 | 10 |
| 10 | `10-connector-plugin-governance` | 23 | 7 | 7 | 9 |
| 11 | `11-risk-tiered-rollout` | 19 | 11 | 3 | 5 |
| 12 | `12-regulatory-compliance-dashboard` | 10 | 2 | 6 | 2 |
| 13 | `13-dora-resilience-monitor` | 14 | 8 | 2 | 4 |
| 14 | `14-communication-compliance-config` | 19 | 6 | 5 | 8 |
| 15 | `15-pages-notebooks-gap-monitor` | 8 | 4 | 2 | 2 |
| 16 | `16-item-level-oversharing-scanner` | 18 | 12 | 3 | 3 |
| 17 | `17-sharepoint-permissions-drift` | 25 | 15 | 6 | 4 |
| 18 | `18-entra-access-reviews` | 17 | 11 | 3 | 3 |
| 19 | `19-copilot-tuning-governance` | 18 | 4 | 4 | 10 |
| 20 | `20-generative-ai-model-governance-monitor` | 14 | 2 | 3 | 9 |
| 21 | `21-cross-tenant-agent-federation-auditor` | 10 | 5 | 1 | 4 |
| 22 | `22-pages-notebooks-retention-tracker` | 14 | 4 | 2 | 8 |
| 23 | `23-copilot-studio-lifecycle-tracker` | 6 | 3 | 2 | 1 |

## VERIFIED actionable items (fix in PRs)

These items have been independently verified. They will become commits in per-solution PRs after user approval of the branch convention.

### 01-copilot-readiness-scanner — 1 actionable

- **Finding #3 [LOW] VERIFIED-BUG** — The text states the package file "aligns to `../../data/evidence-schema.json`". From the docs/ folder, `../../` resolves to the `solutions/` directory, not the repository root. The correct relative pa...
  - *Verification:* From the docs folder, ../../data resolves under solutions/, not the repository data folder. The text should either use the docs-relative ../../../data path or explicitly say it is relative to the solu...

### 03-sensitivity-label-auditor — 4 actionable

- **2 [MEDIUM] VERIFIED-VERSION-DRIFT** — The `regulations` array includes SEC 17a-3, SEC Reg S-P, SOX 302/404, and FFIEC IT Handbook, but these are not documented anywhere in the solution docs or README.
  - *Verification:* The config lists eight regulations, while README regulatory alignment documents only FINRA 4511, SEC 17a-4, GDPR, and GLBA 501(b). Search of this solution's docs did not find SEC 17a-3, SEC Reg S-P, S...
- **10 [LOW] VERIFIED-BUG** — `maxItemsPerScan` value of `-1` in regulated.json means "unlimited" but this convention is not documented in the configs or script help.
  - *Verification:* The regulated tier uses `-1`, and Monitor-Compliance treats negative item limits as no cap against representative base totals. Script help/docs found in the reviewed lines do not explain that conventi...
- **6 [LOW] VERIFIED-BUG** — `PnP.PowerShell` is listed as a required module but is not used by any script in the solution.
  - *Verification:* `PnP.PowerShell` is listed as required, but a solution search found no PnP cmdlets or imports in scripts. This overstates prerequisites for the current scaffold.
- **7 [LOW] VERIFIED-BUG** — `Resolve-OptionalPath` returns inconsistent path formats (absolute if path exists, relative if not).
  - *Verification:* The helper normalizes existing paths but returns the raw input for missing paths. Callers record those values in dependency output, so the format can differ by existence state.

### 04-finra-supervision-workflow — 6 actionable

- **HIGH-002 [HIGH] VERIFIED-BUG** — Evidence output names (`supervision-review-log.csv`, `principal-attestation-summary.json`) do not match the solution contract (`supervision-queue-snapshot`, `review-disposition-log`, `sampling-summary...
  - *Verification:* The flow sample lists output names that do not match the solution contract in README/default-config/evidence-schema: supervision-queue-snapshot, review-disposition-log, sampling-summary.
- **HIGH-003 [HIGH] VERIFIED-BUG** — SHA-256 hashes in the evidence package and companion files do not match actual file content on disk. The evidence integrity chain is broken for the committed artifacts.
  - *Verification:* Get-FileHash computed 019a8f20058fbdc333bf52c5fdfa71075a0648e3372e7e7a8389204fa9e18b19 for this artifact, not the recorded hash. The package, review log, sampling summary, and queue snapshot hashes al...
- **HIGH-004 [HIGH] VERIFIED-BUG** — Retention period values (365/1095/2555 days) lack explicit citation of the FINRA/SEC regulatory basis. For regulated-tier P0 solutions, the retention rationale must be documented.
  - *Verification:* The retention table and tier config values provide 365/1095/2555 days but no explicit FINRA/SEC or firm-policy rationale. For a P0 regulated evidence workflow this is an undocumented basis, not scaffo...
- **MEDIUM-001 [MEDIUM] VERIFIED-VERSION-DRIFT** — exportVersion "1.0.0" does not match the current shared module version "1.1.0". Committed artifacts are stale.
  - *Verification:* The committed evidence package says exportVersion 1.0.0, while scripts\common\IntegrationConfig.psm1 returns the current evidence schema version 1.1.0. The artifact is stale.
- **MEDIUM-004 [MEDIUM] VERIFIED-BUG** — "Append Supervision Evidence Row" uses `sharepoint_create_item` type, contradicting the Dataverse-centric architecture described in all other documentation.
  - *Verification:* The sample action uses SharePoint, while the README/architecture describe Dataverse queue and log tables as the workflow data layer. That mismatch is real even for a representative flow contract.
- **LOW-003 [LOW] VERIFIED-BUG** — ASCII diagram has inconsistent box widths. Cosmetic only.
  - *Verification:* The diagram line lengths are inconsistent (verified at 65, 66, and 67 characters across the box lines). This is cosmetic, but the formatting defect is real.

### 05-dlp-policy-governance — 1 actionable

- **7 [INFO] VERIFIED-DEAD-CONFIG** — The `defaults.baselineStoragePath` uses a Windows-style relative path (`.\\artifacts\\dlp-policy-baseline.json`). While this is only used as documentation/metadata and not consumed by scripts (which c...
  - *Verification:* The field is defined in default-config.json, and repository search within this solution found no script or doc references other than this definition. That makes it unused configuration metadata and po...

### 06-audit-trail-manager — 2 actionable

- **F-05 [LOW] VERIFIED-BUG** — The `$summaryStatus` at line 237 is derived only from `$retentionGapCount`, ignoring the status of controls 3.1, 3.3, 3.11, and 3.12. This could produce a misleading "implemented" overall status when ...
  - *Verification:* The overall evidence summary is based only on retention gaps, while the script later computes statuses for controls 3.1, 3.3, 3.11, and 3.12. This can report implemented even if another control is par...
- **F-11 [INFO] VERIFIED-BUG** — The prerequisites document does not mention PowerShell 7.0+ as a requirement, despite all scripts declaring `#Requires -Version 7.0`.
  - *Verification:* The prerequisites list modules but not the PowerShell runtime version, while all solution scripts declare #Requires -Version 7.0. The docs should state PowerShell 7.0+ before script execution.

### 07-conditional-access-automation — 4 actionable

- **F-08 [MEDIUM] VERIFIED-DEAD-CONFIG** — The `framework_ids` array includes `glba-501b`, `ffiec-it-handbook`, and `nydfs-part-500`, but no documentation, regulatory notes, or evidence outputs reference these frameworks. The `regulations` arr...
  - *Verification:* The extra framework_ids are present, but targeted grep found GLBA/FFIEC/NYDFS only in this config file, not in docs, regulatory notes, scripts, or evidence outputs. That makes them unbacked metadata f...
- **F-09 [MEDIUM] VERIFIED-BUG** — Line 397 hardcodes `controls = @('2.3', '2.6', '2.9')` in the `policyStateDocument`. These should be sourced from `$config.controls` (which reads from the config files) rather than hardcoded, to maint...
  - *Verification:* Export-Evidence already merges `$config.controls`, but the policy-state artifact hardcodes the same control IDs. If config controls change, the artifact can drift from the configuration source of trut...
- **F-04 [LOW] VERIFIED-BUG** — The `$succeeded` variable is set to `$true` on success (line 440) and initialized to `$false` (line 436), but it is never checked after the retry loop completes. If all retries fail, the script contin...
  - *Verification:* The retry loop never checks `$succeeded` after all attempts, and final failure only writes a non-terminating error before moving on. This can leave Graph execution failures untracked in the final resu...
- **F-07 [LOW] VERIFIED-DEAD-CONFIG** — The file contains only `# Temporary file -- safe to delete`. Its purpose appears to be satisfied by the Pester test suite (`07-conditional-access-automation.Tests.ps1`), which already performs syntax ...
  - *Verification:* The file contains only a temporary safe-to-delete comment, and the real Pester suite already performs syntax parsing. A solution search found no in-solution references to parse-check.ps1.

### 09-feature-management-controller — 7 actionable

- **13 [MEDIUM] VERIFIED-BUG** — No test coverage for SHA-256 baseline integrity verification.
  - *Verification:* Monitor-Compliance implements baseline SHA-256 integrity verification, but the tests only cover the unrelated stable feature hash helper and contain no SHA/integrity assertions. The coverage gap is re...
- **15 [MEDIUM] VERIFIED-DEAD-CONFIG** — Rollout ring schema differs from tier configs: default has `defaultState`/`maxAudience` but no `enabled`; tiers have `enabled` but not `defaultState`/`maxAudience`.
  - *Verification:* The rollout ring schemas differ exactly as claimed. The default-only fields are not consumed by the rollout code, so they are effectively metadata/dead config unless schema documentation is clarified.
- **19 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — Controls 4.3 and 4.4 implementation descriptions do not align with the actual workloads (Teams Phone/Queues and Viva Suite) that these controls govern. No specific features for these workloads exist i...
  - *Verification:* The README describes support for Teams Phone/Queues and Viva Suite controls, but the tier configs do not define phone/queue or Viva-specific feature records. That is an overclaim relative to the actua...
- **22 [MEDIUM] VERIFIED-BUG** — Monitor-Compliance validation command passes raw tier config as baseline path rather than a generated baseline artifact.
  - *Verification:* The validation checklist points Monitor-Compliance at a raw tier config rather than the generated baseline artifact produced by Deploy-Solution. That bypasses the intended artifact/hash validation flo...
- **9 [MEDIUM] VERIFIED-BUG** — Second `.EXAMPLE` shows raw tier config as `-BaselinePath`, which is misleading about the intended input.
  - *Verification:* The second help example passes a tier configuration as BaselinePath, while the first example and parameter text indicate an approved feature-state baseline artifact. This is misleading for operators.
- **14 [LOW] VERIFIED-BUG** — No test for `Get-PowerAutomateFlowPlan` with BaselineOnly switch behavior.
  - *Verification:* Get-PowerAutomateFlowPlan has BaselineOnly-specific behavior, but the test file has no test for that function or switch path. The claimed coverage gap is verified.
- **7 [INFO] VERIFIED-BUG** — `Export-SolutionEvidencePackage` called without `-ExpectedArtifacts`, so artifact completeness is not validated.
  - *Verification:* The solution call omits -ExpectedArtifacts even though the shared exporter supports artifact completeness validation. As written, the package validates shape/hash but not that all documented artifact ...

### 10-connector-plugin-governance — 7 actionable

- **1 [MEDIUM] VERIFIED-BUG** — DataverseUrl regex rejects sovereign cloud URLs (`.cn`, `.de`, `.us`) and URLs with trailing paths beyond a single `/`. Pattern should be broadened to `'^https://[\w\-\.]+\.dynamics\.\w+/?.*$'` or sim...
  - *Verification:* The cited validation pattern is restricted to dynamics.com and an optional trailing slash, so sovereign Dataverse hosts and URLs with paths are rejected before the script can run.
- **20 [MEDIUM] VERIFIED-VERSION-DRIFT** — References control 2.16 as "documentation-only" but this control does not appear in any config file's `controls` array. Either add 2.16 to configs or remove the reference.
  - *Verification:* The README states 2.16 is listed in metadata, but the config metadata read for this solution omits it. That is catalog/config drift rather than a script runtime bug.
- **10 [LOW] VERIFIED-BUG** — Attestation expiration uses `yyyy-MM-dd` format while Deploy-Solution uses ISO 8601 `o` format.
  - *Verification:* The two scripts emit the same evidence field using different date formats. That creates inconsistent artifact shape for downstream evidence consumers.
- **13 [LOW] VERIFIED-BUG** — Fallback inventory differs from Deploy-Solution inventory. Missing ServiceNow and Teams connectors.
  - *Verification:* The monitoring fallback sample does not match the deployment sample inventory, so a fresh monitoring run without artifacts evaluates a different population. That is an internal scaffold inconsistency.
- **19 [LOW] VERIFIED-DEAD-CONFIG** — `autoApprovedConnectorIds` is pulled from `$config.Baseline` in Deploy-Solution (line 392), not from the active tier config. This works but the indirection is non-obvious.
  - *Verification:* Deploy-Solution.ps1 always pulls autoApprovedConnectorIds from baseline, so the same field in recommended.json is not consumed by the deployment classifier. That makes the recommended-tier list dead o...
- **5 [LOW] VERIFIED-DEAD-CONFIG** — Attestation expiration uses hardcoded 365/180 day values instead of reading from tier config `evidenceRetentionDays` or a dedicated config property.
  - *Verification:* Attestation expiration is hardcoded while tier configs define evidenceRetentionDays, including baseline 90, recommended 180, and regulated 365. That config is not consumed by the attestation logic.
- **7 [LOW] VERIFIED-BUG** — Evidence inventory (4 connectors) does not match Deploy-Solution inventory (6 connectors). Missing: Teams, ServiceNow.
  - *Verification:* The representative inventories are inconsistent across scripts, so evidence export omits two sample connectors produced by deployment. Documentation-first sample data should still be internally consis...

### 11-risk-tiered-rollout — 11 actionable

- **F-02 [MEDIUM] VERIFIED-BUG** — The `trainingCompletion` gate criterion (Monitor-Compliance.ps1, line 188) maps to a boolean feature flag (`vivaInsightsEnabled`) rather than actual training completion data. This produces a meaningle...
  - *Verification:* The trainingCompletion gate is satisfied by the analytics feature flag, not by training completion records. That can pass the gate without any training evidence.
- **F-03 [MEDIUM] VERIFIED-BUG** — The `doraResilienceReview` gate criterion (Monitor-Compliance.ps1, line 210) evaluates a configuration flag (`doraGateRequired`) rather than evidence of a completed review. This auto-passes for regula...
  - *Verification:* The monitor treats doraGateRequired as proof the review is complete. Regulated config sets that flag true, so the criterion can auto-pass without review evidence.
- **F-05 [LOW] VERIFIED-BUG** — The `serviceHealthGreen` and `incidentThreshold` gate criteria both compare blocked-user count against `maximumOpenIncidents`. Blocked users and open incidents are different concepts; the semantic map...
  - *Verification:* Both gates compare blocked-user count to maximumOpenIncidents even though service health, incidents, and blocked users are different concepts. The semantic mismatch is real.
- **F-06 [LOW] VERIFIED-BUG** — The `backlogWithinThreshold` gate criterion reuses the identical logic as `incidentThreshold`, making two gates that always produce the same result.
  - *Verification:* backlogWithinThreshold reuses the same blocked-user/open-incident threshold logic as incidentThreshold. The two gates will always produce the same pass/fail result for a wave.
- **F-07 [LOW] VERIFIED-DEAD-CONFIG** — The `manualReviewRequiredTiers` field in baseline.json is never read by any script. It exists as dead configuration that could mislead operators.
  - *Verification:* The field exists in baseline config, and repository search found no script reference outside that definition. It is not consumed by rollout, monitoring, or evidence logic.
- **F-08 [LOW] VERIFIED-DEAD-CONFIG** — The `auditTrail.fullAuditTrailRequired` field in regulated.json is never read by any script. The audit trail check uses per-user flags instead.
  - *Verification:* Search found fullAuditTrailRequired only in regulated config. Monitor-Compliance evaluates auditTrailVerified from per-user AuditTrailReady blockers, not this config field.
- **F-09 [LOW] VERIFIED-BUG** — Architecture doc references "License Assigner" component but the diagram shows "Assignment Staging". Naming mismatch.
  - *Verification:* The diagram labels the component Assignment Staging, while the explanatory note names License Assigner. That is a real documentation naming mismatch.
- **F-10 [LOW] VERIFIED-VERSION-DRIFT** — Architecture doc states Wave 0 targets 50 users but baseline.json overrides this to 25. Doc should note tier-specific sizing.
  - *Verification:* The architecture describes Wave 0 as 50 users, but baseline config overrides Wave 0 to 25 users. The doc mentions tier availability but not tier-specific sizing.
- **F-11 [LOW] VERIFIED-DOC-OVERCLAIM** — Architecture doc lists "reserved Copilot seats available" as a Wave 0 gate criterion, but no script implements seat-availability checking.
  - *Verification:* The architecture lists seat availability as a Wave 0 gate, but the implemented gate switch has no seat inventory or availability check. That overstates what the scaffold gate evaluator covers.
- **F-12 [LOW] VERIFIED-VERSION-DRIFT** — Evidence-export.md field tables omit `runtimeMode`, `dataSourceMode`, `warning`, and `generatedAt` fields that are present in actual output.
  - *Verification:* The evidence-export field tables omit fields present in the actual script output for readiness, approvals, dashboard, and package metadata. This is documentation/schema drift.
- **F-16 [INFO] VERIFIED-VERSION-DRIFT** — Prerequisites doc does not mention the shared PowerShell modules (IntegrationConfig.psm1, EvidenceExport.psm1) as dependencies.
  - *Verification:* The prerequisite doc omits shared repository modules that the scripts import. That is dependency documentation drift.

### 12-regulatory-compliance-dashboard — 2 actionable

- **Finding 2: Baseline Tier Config Missing  [LOW] VERIFIED-DEAD-CONFIG** — The `regulated.json` config includes `"freshnessCheckCadence": "hourly"` and `default-config.json` includes it in `defaults`, but `baseline.json` omits this key entirely. `recommended.json` also omits...
  - *Verification:* default-config.json defines freshnessCheckCadence under defaults and regulated.json defines it at top level, while baseline.json and recommended.json omit it. Grep found no script references, so the i...
- **Finding 3: default-config.json framework [LOW] VERIFIED-DEAD-CONFIG** — The `framework_ids` array lists 13 framework identifiers, but only 7 are used in tier configurations and documentation. The additional 6 (sec-reg-sp, sr-11-7, ffiec-it-handbook, finra-4530, eu-ai-act,...
  - *Verification:* The file contains 13 framework_ids, but regulatoryFrameworks lists only 7 and grep showed the 6 extra IDs only appear in default-config.json. That makes the extras unused/dead config unless they are e...

### 13-dora-resilience-monitor — 8 actionable

- **F-01 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — DORA Art. 17 alignment section incorrectly attributes resilience testing to Art. 17. Resilience testing is governed by Art. 24-26. Art. 17 covers ICT-related incident management process only.
  - *Verification:* The Art. 17 alignment note explicitly includes resilience-test readiness under Art. 17. That overstates the incident-management article; resilience testing should be separated from Art. 17 alignment.
- **F-02 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — Regulatory Alignment section references Art. 17 and Art. 18 but omits Art. 19, which governs regulatory reporting of major incidents -- a feature the solution explicitly supports at the regulated tier...
  - *Verification:* The README regulatory alignment cites only Art. 17 and Art. 18, while the solution also describes regulated-tier notification/reporting support elsewhere. The reporting-package claim should include Ar...
- **F-03 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — Art. 19 is correctly referenced for the reporting package, but this creates an inconsistency with architecture.md and README.md, which attribute reporting to Art. 17. All documentation should consiste...
  - *Verification:* The evidence-export guide correctly uses Art. 19 for reporting-package preparation. That conflicts with README/architecture wording that folds reporting timestamps/workflows into Art. 17.
- **F-07 [LOW] VERIFIED-BUG** — Line 147: `regulatoryNotificationRequired` uses exact equality (`$incident.severity -eq $regulatoryNotificationThreshold`) rather than a severity-rank comparison. If the threshold is "major" (the defa...
  - *Verification:* The code uses exact equality for a configurable threshold. The default major threshold works, but changing the threshold to significant would fail to flag higher-ranked major incidents.
- **F-08 [LOW] VERIFIED-DEAD-CONFIG** — `notificationWindowHours` (4) and `rcaWindowDays` (30) are defined but not consumed by any script logic. These should either be consumed or documented as reference-only configuration values.
  - *Verification:* These fields are present in regulated.json, and repository search found no script references beyond the config definition. They are currently reference-only unless documented or consumed.
- **F-09 [LOW] VERIFIED-DEAD-CONFIG** — `defaults.healthPollingIntervalMinutes` key name differs from tier configs' `serviceHealthPollingIntervalMinutes`. The default value is never used because `Get-DrmConfiguration` returns only the tier ...
  - *Verification:* The default uses healthPollingIntervalMinutes, while Get-DrmConfiguration only returns the tier serviceHealthPollingIntervalMinutes value. The default key is not consumed by current scripts.
- **F-11 [LOW] VERIFIED-DOC-OVERCLAIM** — The DORA severity classification model covers Art. 18 criteria (a), (c), and (f) but does not model criteria (b) reputational impact, (d) geographical spread, (e) data losses, or (g) economic impact. ...
  - *Verification:* The documented classifier only describes downtime, user impact, degradation, and logging outcomes. It does not disclose that other DORA Art. 18 criteria are outside the scaffolded model.
- **F-12 [INFO] VERIFIED-DOC-OVERCLAIM** — The DORA regulatory notification timeline only models the initial 4-hour deadline. The intermediate (72-hour) and final (1-month) reporting deadlines from Commission Delegated Regulation (EU) 2024/177...
  - *Verification:* The regulated config models an initial notification window and RCA tracking but has no intermediate or final reporting deadline fields. The adjacent 'Full DORA incident reporting' description is broad...

### 14-communication-compliance-config — 6 actionable

- **H-02 [HIGH] VERIFIED-BUG** — No tests validate the output structure of `Deploy-Solution.ps1`, `Export-Evidence.ps1`, or `Monitor-Compliance.ps1`. No test validates tier progression invariants (template count increases across tier...
  - *Verification:* I read the full test file; it checks file presence, minimal config, syntax, documentation strings, and CCC-Common helpers. It does not invoke Deploy-Solution.ps1, Export-Evidence.ps1, or Monitor-Compl...
- **M-02 [MEDIUM] VERIFIED-BUG** — Field names `wordAdded` / `wordRemoved` in the script use singular naming while the documentation implies plural array fields. Naming inconsistency between code and documentation.
  - *Verification:* The script property names omit brackets while the documentation presents bracketed field names in the Field column. Even if intended as array notation, it can be misread as literal JSON property names...
- **M-07 [MEDIUM] VERIFIED-DEAD-CONFIG** — `queueHealthThresholds` defined in config but never consumed by any script. Dead configuration.
  - *Verification:* The thresholds are defined in default config, and repository search found no script or doc consumer besides this definition. Monitor-Compliance.ps1 hardcodes queueHealthy false and never evaluates the...
- **L-02 [LOW] VERIFIED-BUG** — `credentialSupplied` expression may produce unexpected results with zero-length SecureString. Should use explicit null and length check.
  - *Verification:* The expression treats any SecureString object as supplied and does not check whether it has content. A zero-length SecureString can therefore report credentials supplied when no usable secret exists.
- **L-05 [LOW] VERIFIED-DEAD-CONFIG** — `allCopilotContentMonitored: true` is defined but never checked by any script. Documentation-only marker with no programmatic effect.
  - *Verification:* The regulated config defines allCopilotContentMonitored, and search found no other occurrence in scripts or docs. It currently has no programmatic effect.
- **I-04 [INFO] VERIFIED-BUG** — Minor grammatical issue: "Evidence export packages generated content" should be "Evidence export packages contain generated content."
  - *Verification:* The sentence is grammatically incorrect as cited. It should say the packages contain generated content or that they package generated content.

### 15-pages-notebooks-gap-monitor — 4 actionable

- **F-01 [MEDIUM] VERIFIED-BUG** — - Files: - `/mnt/workspace/working/fsi-solutions/FSI-CopilotGov-Solutions/solutions/15-pages-notebooks-gap-monitor/scripts/Deploy-Solution.ps1` (lines 80, 94, 108, 122, 136) -- uses `regulations` - `/...
  - *Verification:* The cited files really use two different field names for per-gap regulation data. That creates schema drift for downstream consumers of gap findings versus deployment/monitoring output.
- **F-02 [LOW] VERIFIED-DEAD-CONFIG** — `framework_ids` includes `"sec-17a3"` and `"glba-501b"` which are never referenced in scripts, gap definitions, control mappings, or documentation text.
  - *Verification:* Repository search within the solution found `sec-17a3`, `glba-501b`, and `framework_ids` only in this config block. They are therefore not consumed by scripts or documented mappings in this solution.
- **F-04 [LOW] VERIFIED-BUG** — - File: `/mnt/workspace/working/fsi-solutions/FSI-CopilotGov-Solutions/solutions/15-pages-notebooks-gap-monitor/scripts/Monitor-Compliance.ps1` (line 47: `[CmdletBinding()]`, line 348: writes file unc...
  - *Verification:* The monitor script lacks `SupportsShouldProcess` while the deploy and export scripts include it, and it writes the status file unconditionally. Users cannot preview this write with `-WhatIf`.
- **F-07 [INFO] VERIFIED-BUG** — The `elseif ($controlsRequired) { 'planned' } else { 'planned' }` branches produce identical output.
  - *Verification:* The cited branch really returns `planned` for both non-regulated controls-required and controls-not-required cases. This is redundant logic and may hide a missing intended status distinction.

### 16-item-level-oversharing-scanner — 12 actionable

- **F1 [HIGH] VERIFIED-DEAD-CONFIG** — `riskThresholds` configuration values (high:70, medium:40, low:0) are defined but never used by the Get-RiskTier function, which uses hardcoded string-matching logic instead. The two scoring systems (...
  - *Verification:* The default riskThresholds values are not read on the scoring path; tier assignment is hardcoded while WeightedScore is calculated separately from base scores and weights. That can produce inconsisten...
- **F3 [HIGH] VERIFIED-BUG** — Evidence export re-runs the entire scan/score pipeline via Monitor-Compliance.ps1 instead of packaging existing artifacts. This overwrites prior scan data and couples evidence packaging to live scanni...
  - *Verification:* Export-Evidence invokes Monitor-Compliance instead of accepting existing scan, score, or remediation artifacts. Monitor-Compliance writes scan and scored outputs to fixed artifact paths when ExportPat...
- **F10 [MEDIUM] VERIFIED-BUG** — TenantUrl parameter is declared as Mandatory but is never referenced in script logic. Users are forced to provide a value that is discarded.
  - *Verification:* The parameter is required from callers but is not used by either script's execution path. In a sample-data scaffold, that makes the parameter friction without behavior.
- **F4 [MEDIUM] VERIFIED-BUG** — Content-category keyword matching on item paths causes site-URL false positives. Items on `/sites/legal/` are all categorized as "legalDocuments" regardless of actual content, inflating their weighted...
  - *Verification:* Because ItemPath includes the site path, a site such as /sites/legal/ can match the legalDocuments keyword even when the item content or label is unrelated. The generated sample report shows this effe...
- **F6 [MEDIUM] VERIFIED-BUG** — Items with unrecognized remediation modes are silently skipped with no warning log entry. The action index increments but no record is created.
  - *Verification:* If mode is neither approval-gate nor auto-remediate, the loop falls through without warning or remediationLog entry, then increments the action index. That silently drops the item from output records.
- **F7 [MEDIUM] VERIFIED-DEAD-CONFIG** — maxSitesPerRun and maxItemsPerLibrary configuration values are defined in all tier configs but never enforced in any script.
  - *Verification:* The maxSitesPerRun and maxItemsPerLibrary values exist in every tier config, yet the scan and monitor path does not read or enforce them. Deploy-Solution only records them in a manifest, so they are d...
- **F8 [MEDIUM] VERIFIED-BUG** — WeightedScore values are strings when imported from CSV. Sort-Object in Export-Evidence.ps1 (line 227) sorts lexicographically, producing incorrect sort order for scores like "117" vs "90".
  - *Verification:* Import-Csv returns WeightedScore as a string, and the remediation action sort uses the original imported objects. Numeric scores such as 117 and 90 can therefore be ordered lexicographically instead o...
- **F11 [LOW] VERIFIED-DEAD-CONFIG** — OneDrive added to scanWorkloads but no script has OneDrive-specific scanning logic or PnP patterns for per-user drive enumeration.
  - *Verification:* OneDrive is present in recommended and regulated scanWorkloads, but the runtime path never reads scanWorkloads or branches to OneDrive enumeration. The configuration currently has no effect.
- **F13 [LOW] VERIFIED-DOC-OVERCLAIM** — Control 1.14 is mentioned as "documentation-only" in the Related Controls section but does not appear in any configuration file's controls array.
  - *Verification:* The README claims control 1.14 is listed in metadata, but the solution configuration metadata does not include it. That is coverage documentation drift.
- **F14 [LOW] VERIFIED-DOC-OVERCLAIM** — BroadGroup with sensitive labels being classified as MEDIUM is not documented in the risk classification description.
  - *Verification:* The architecture describes broad group access as LOW only when no sensitive label is present and the table lists BroadGroup as LOW. It omits the actual BroadGroup+sensitive-label MEDIUM branch.
- **F15 [LOW] VERIFIED-DOC-OVERCLAIM** — Microsoft.Graph module listed as required but no script directly uses Microsoft.Graph cmdlets.
  - *Verification:* The module requirement is overstated for the current scaffold because scripts import shared modules and use sample data, but no Microsoft.Graph cmdlet appears in solution scripts. This should be optio...
- **F18 [INFO] VERIFIED-BUG** — Sample filenames reference year "2025" while dates are dynamically generated. Minor cosmetic inconsistency.
  - *Verification:* The sample filenames contain a fixed 2025 year while the dates are generated relative to the current run date. This is a minor cosmetic inconsistency in representative sample data.

### 17-sharepoint-permissions-drift — 15 actionable

- **1 [CRITICAL] VERIFIED-DOC-OVERCLAIM** — Drift scan never captures live current state -- always falls back to sample data even when baseline exists. The `Compare-PermissionSets` function is implemented but never invoked in the main logic.
  - *Verification:* The code defines Compare-PermissionSets but main logic uses Get-SampleDriftData even with a baseline. README has scaffold warnings, but architecture still asserts live current-state capture/comparison...
- **2 [HIGH] VERIFIED-DEAD-CONFIG** — Risk scoring hardcodes weights instead of reading from `default-config.json` `driftTypeWeights`. Configuration changes have no effect on scoring.
  - *Verification:* The script never reads ConfigPath/default-config for scoring weights, so configuration changes cannot affect scoring. This is dead configuration rather than a live API defect.
- **3 [HIGH] VERIFIED-BUG** — Graph API `sendMail` endpoint uses `$Recipient` or `$approver` as the sender identity. This means the alert is sent FROM the recipient TO the recipient, which is semantically wrong and would require t...
  - *Verification:* The Graph user segment is the sender mailbox, but the code uses the recipient/approver as that sender and recipient. Since the functions purport to send when Graph context exists, this is a real notif...
- **5 [HIGH] VERIFIED-DOC-OVERCLAIM** — Live PnP enumeration uses incorrect approach for `HasUniqueRoleAssignments` detection. `Get-PnPListItem -Fields 'HasUniqueRoleAssignments'` treats it as a field rather than a property.
  - *Verification:* The cited placeholder pattern exists, but the code is clearly not complete live enumeration. Architecture still says baseline capture connects via PnP and snapshots permissions, so treat this as doc o...
- **10 [MEDIUM] VERIFIED-BUG** — `pending-approvals.json` is overwritten on each run, losing previously pending items.
  - *Verification:* Existing pending approvals are never loaded or merged, so a later run can replace still-pending records. This is a real workflow state bug even for file-based scaffold artifacts.
- **11 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — No mechanism exists to receive, process, or enforce approval responses or timeout escalations.
  - *Verification:* The script queues records and sends email only; no processor handles approve/deny responses or deadline escalation. Since architecture asserts escalation behavior, this is a doc overclaim unless an ex...
- **14 [MEDIUM] VERIFIED-BUG** — Module version comparison is not performed -- reports "Available: True" regardless of installed version.
  - *Verification:* Deploy claims prerequisite validation but only checks module presence, so outdated modules are reported as available. This is a validation bug independent of live API use.
- **15 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — Documents risk scoring weights and Solution 02 integration that are not implemented.
  - *Verification:* Documented risk factors and Solution 02 scoring context are not implemented; Deploy only checks whether the Solution 02 folder exists. The architecture overstates implemented scoring behavior.
- **7 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — Drift comparison only evaluates `uniquePermissions`. Changes to `sharingSettings`, `sharingLinks`, and `externalUsers` (three of four baseline dimensions) are not compared, undermining controls 1.2 an...
  - *Verification:* The comparison function ignores sharingSettings, sharingLinks, and externalUsers, while docs/control text says those dimensions are detected. Even if the scan is sample-first today, the documented cov...
- **8 [MEDIUM] VERIFIED-BUG** — Sample drift data has a scoring discrepancy: External Consultant scenario hardcodes `RiskScore = 75` but `Get-DriftTypeWeight` for ADDED + Contribute + ExternalUser computes 45.
  - *Verification:* The sample scenario's hard-coded score/tier does not match the script's own scoring function. This can mislead validation and documentation even in scaffold mode.
- **19 [LOW] VERIFIED-DOC-OVERCLAIM** — Live code path returns empty arrays for `sharingLinks` and `externalUsers`.
  - *Verification:* The code does not capture sharing links or external users in the live placeholder while docs list them as baseline dimensions. That is a documentation overclaim until implemented.
- **20 [LOW] VERIFIED-BUG** — CSV export does not include SHA-256 hash file, breaking evidence integrity chain.
  - *Verification:* drift-findings.csv is an evidence artifact but no companion hash is generated. This contradicts the evidence integrity documentation and should be fixed or documented as a convenience export.
- **22 [LOW] VERIFIED-DOC-OVERCLAIM** — References `drift-report-latest.json` which is never created (no pointer file for drift reports).
  - *Verification:* No latest pointer/copy is created by Invoke-DriftScan, so copy-pasted docs can reference a missing report. Export-Evidence discovers the latest timestamped report, but the guide examples are stale.
- **23 [LOW] VERIFIED-VERSION-DRIFT** — Control 1.15 mentioned in coverage state but not documented in controls table.
  - *Verification:* README's coverage state is stale relative to the controls table and machine-readable config. This is documentation/version drift rather than an implementation bug.
- **24 [LOW] VERIFIED-DEAD-CONFIG** — `requireApprovalForHighRisk` and `enableSiteOwnerNotifications` flags are defined but not consumed by any script.
  - *Verification:* Search across the solution found these flags only in tier configs, not in script logic. They are dead configuration unless intended solely as documentation hints.

### 18-entra-access-reviews — 11 actionable

- **1 [MEDIUM] VERIFIED-BUG** — Evidence export hardcodes `ConfigurationTier = 'baseline'` instead of forwarding the user's selected tier. Evidence packages will always report baseline regardless of actual tier.
  - *Verification:* The orchestrator hardcodes baseline when calling Export-Evidence and has no ConfigurationTier parameter to forward. Orchestrated evidence can therefore report the wrong tier after a non-baseline deplo...
- **2 [MEDIUM] VERIFIED-DEAD-CONFIG** — Config file defines multi-reviewer and escalation chain patterns but no script imports or uses it. Creates a capability gap between configuration and implementation.
  - *Verification:* The mapping defines reviewer and escalation data, but repo search found references only in docs/tests, not runtime scripts. New-AccessReview uses review-schedule fallbackReviewer instead.
- **4 [MEDIUM] VERIFIED-BUG** — No confirmation prompt (`-Confirm` / `ShouldProcess`) before applying deny decisions that remove user access. Safety concern for regulated environments.
  - *Verification:* The live path posts applyDecisions without SupportsShouldProcess, ConfirmImpact, ShouldProcess, or ShouldContinue. That bypasses the expected safety prompt for access-removal operations.
- **10 [LOW] VERIFIED-DOC-OVERCLAIM** — States `reminderDays` in review-schedule.json controls expiry threshold, but scripts use hardcoded `$ThresholdHours = 48` parameter. Config setting has no effect.
  - *Verification:* The doc says reminderDays controls the expiry threshold, but Get-ReviewResults uses a hardcoded ThresholdHours default and never loads review-schedule.json. The configuration setting has no runtime ef...
- **11 [LOW] VERIFIED-DOC-OVERCLAIM** — Lists `PnP.PowerShell` as a required module, but no script imports or uses it.
  - *Verification:* The prerequisites require PnP.PowerShell, but a solution-wide search found no PnP imports or cmdlet usage. That dependency is overstated for the current scaffold.
- **12 [LOW] VERIFIED-BUG** — Action mapping only handles `Deny` explicitly. Other decision values (`DontKnow`, `NotReviewed`, `Recommendation`) are silently treated as `maintain-access`.
  - *Verification:* Only Deny is handled explicitly; every other decision value is logged as maintain-access. That can misrepresent DontKnow, NotReviewed, recommendations, or unexpected Graph values in evidence.
- **13 [LOW] VERIFIED-BUG** — `siteUrl` field is not populated in live Graph API results, only in sample data path. Creates incomplete evidence records.
  - *Verification:* The live appliedActions object omits siteUrl, while the sample path and evidence documentation include it. Live evidence records would therefore be incomplete for the documented applied-actions schema...
- **14 [LOW] VERIFIED-DEAD-CONFIG** — `requireOwnerAttestation: true` and `requireExaminerReadyEvidence: true` flags are defined but not enforced by any script.
  - *Verification:* The regulated tier defines these enforcement flags, but solution-wide search found references only in JSON config files. No script enforces or reports these requirements.
- **5 [LOW] VERIFIED-BUG** — `fallbackReviewer: "compliance-officer"` is a role name, not a resolvable UPN or object ID. Graph API `/users/compliance-officer` would fail. Should be an email like `compliance-officer@contoso.com`.
  - *Verification:* New-AccessReview constructs reviewer queries as /users/$reviewer, so the fallback value must be a resolvable user id or UPN. The role label compliance-officer would not resolve as a Graph user by defa...
- **8 [LOW] VERIFIED-DEAD-CONFIG** — `maxSitesPerRun` from tier config is not enforced. Script processes all sites regardless of tier setting.
  - *Verification:* New-AccessReview iterates all sites by tier and never reads maxSitesPerRun; that setting is only captured in the deployment manifest. The configured run limit has no effect on review creation.
- **16 [INFO] VERIFIED-DOC-OVERCLAIM** — Orchestrator description says "Create access reviews for HIGH-risk sites" but the orchestrator creates reviews for all tiers.
  - *Verification:* The architecture summary says the orchestrator creates HIGH-risk reviews, but New-AccessReview loops HIGH, MEDIUM, and LOW and the orchestrator does not filter. The documented scope is inaccurate for ...

### 19-copilot-tuning-governance — 4 actionable

- **F04 [MEDIUM] VERIFIED-BUG** — References to `docs\getting-started\prerequisites.md`, `docs\operational-handbook.md`, etc. that may not exist at the expected relative paths
  - *Verification:* The checklist references solution-relative docs paths that do not exist; Test-Path returned false for the cited files. These are broken documentation references, not just missing optional content.
- **F05 [LOW] VERIFIED-DEAD-CONFIG** — License threshold value (5,000) hardcoded in notes string instead of reading from `$configuration.minimumLicenseThreshold`
  - *Verification:* The script accepts a Configuration object but hardcodes the 5,000 threshold text instead of using default-config.json minimumLicenseThreshold. This creates config drift if the threshold changes.
- **F12 [LOW] VERIFIED-BUG** — TenantId parameter validated as non-empty string but not as GUID format
  - *Verification:* The parameter help says TenantId is a GUID, but the parameter only checks for a non-empty string. Invalid tenant labels can therefore be written into deployment artifacts.
- **F13 [LOW] VERIFIED-DOC-OVERCLAIM** — Lists `Microsoft.Graph` as required module but scripts do not use it in documentation-first form; could note this is forward-looking
  - *Verification:* The prerequisite marks Microsoft.Graph as required, but grep found no Graph cmdlet use in the solution scripts. In a documentation-first scaffold, this overstates the current runtime dependency.

### 20-generative-ai-model-governance-monitor — 2 actionable

- **F-10 [HIGH] VERIFIED-BUG** — `regulations` and `framework_ids` arrays are inconsistent with README, DELIVERY-CHECKLIST, architecture docs, and all script comment headers. Missing: SR 26-2, OCC 2026-13, NIST AI RMF 1.0, ISO/IEC 42...
  - *Verification:* default-config.json omits sr-26-2, occ-2026-13, nist-ai-rmf, and iso-iec-42001 while the delivery checklist says those framework_ids should align with the catalog. Deploy-Solution exports these metada...
- **F-06 [INFO] VERIFIED-BUG** — `New-SampleValidationRecord` hardcodes `independentChallengeStatus = 'not-required'` for all tiers; misleading for the regulated tier where independent challenge is required
  - *Verification:* The validation sample always emits 'not-required' even when regulated.json sets independentChallenge.enabled to true and annualChallengeRequired to true. That makes regulated-tier sample evidence inco...

### 21-cross-tenant-agent-federation-auditor — 5 actionable

- **1 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — Configuration tiers table states regulated retention is "1825 days (7 years)" but 1825 days equals 5 years, not 7. Either change to 2555 days for 7 years or correct the label to "5 years."
  - *Verification:* Verified. The README labels 1825 days as 7 years, but 1825 days is 5 years; the config also uses 1825 as the regulated retention value.
- **3 [MEDIUM] VERIFIED-BUG** — Builds custom evidence package instead of using shared `Export-SolutionEvidencePackage` from `EvidenceExport.psm1`. This creates maintenance risk if the shared evidence schema evolves.
  - *Verification:* Verified. The script imports `IntegrationConfig.psm1` but not the shared `EvidenceExport.psm1`, then manually constructs the evidence package instead of using `Export-SolutionEvidencePackage`.
- **4 [LOW] VERIFIED-DOC-OVERCLAIM** — "Missing required field" section lists `mcpTrustAttestationRequired`, `agentIdSigningRequired`, and `crossTenantAuditLogRetentionDays` as required, but `Test-CtafConfiguration` does not validate these...
  - *Verification:* Verified. The troubleshooting doc lists these fields as required, but `Test-CtafConfiguration` only requires solution metadata, primary controls, cadence, evidence retention, notification mode, publis...
- **5 [LOW] VERIFIED-BUG** — `Test-CtafConfiguration` does not validate `mcpTrustAttestationRequired`, `agentIdSigningRequired`, `agentIdKeyRotationTrackingEnabled`, or `crossTenantAuditLogRetentionDays`, which are consumed by Mo...
  - *Verification:* Partly verified. The validator omits `mcpTrustAttestationRequired`, `agentIdSigningRequired`, `agentIdKeyRotationTrackingEnabled`, and `crossTenantAuditLogRetentionDays`, though the claim is wrong tha...
- **6 [LOW] VERIFIED-DEAD-CONFIG** — `mcpAttestationRevalidationRequired` field is declared but never consumed by `Get-CtafConfiguration` or any script. It is silently dropped during config loading.
  - *Verification:* Verified. `regulated.json` declares `mcpAttestationRevalidationRequired`, but `Get-CtafConfiguration` does not copy that top-level field and repository search found no script consuming it.

### 22-pages-notebooks-retention-tracker — 4 actionable

- **F-04 [MEDIUM] VERIFIED-BUG** — `Test-PnrtConfiguration` checks for field presence and non-null/non-whitespace values but does not validate numeric ranges. A configuration with `"pagesRetentionDays": 0` or `"pagesRetentionDays": -1`...
  - *Verification:* The validator checks presence/null/whitespace and the artifact-type count, but it does not check positive numeric ranges. Negative or zero retention day values could pass validation.
- **F-07 [MEDIUM] VERIFIED-BUG** — Tests validate file presence, config shapes, script syntax, and documentation content but do not invoke the scripts to verify they execute successfully with sample data. Given that these scripts are d...
  - *Verification:* The tests cover file presence, JSON shape, syntax, parameters, and documentation, but they do not execute Deploy-Solution, Monitor-Compliance, or Export-Evidence. Because these scripts run in sample-d...
- **F-10 [LOW] VERIFIED-DEAD-CONFIG** — The baseline config has both `"branchingAuditRequired": true` and `"branchingAuditMode": "summary"`. The field names could imply that branching audit can be disabled (`branchingAuditRequired: false`),...
  - *Verification:* All tier configs set branchingAuditRequired to true, and grep found no behavioral use beyond copying it into the merged configuration. That makes the flag currently dead configuration.
- **F-12 [LOW] VERIFIED-BUG** — The prerequisites list "Microsoft.Graph modules for Notes and Sites" without specifying the exact module names (`Microsoft.Graph.Notes`, `Microsoft.Graph.Sites`). The Microsoft Graph PowerShell SDK us...
  - *Verification:* The prerequisite is imprecise: it names functional areas rather than the actual Microsoft Graph PowerShell submodules. This is a documentation defect, not an API overclaim.

### 23-copilot-studio-lifecycle-tracker — 3 actionable

- **F-01 [MEDIUM] VERIFIED-DOC-OVERCLAIM** — The evidence-export.md documentation states: "scripts/Export-Evidence.ps1 calls the shared `Export-SolutionEvidencePackage` function to create the final package aligned to `data/evidence-schema.json`....
  - *Verification:* The cited doc claim is false: the script builds `$package` inline and writes it directly. This is directly verifiable from code and is not one of the known-unverifiable version-history/rollback items.
- **F-02 [MEDIUM] VERIFIED-BUG** — Export-Evidence.ps1 constructs the evidence package inline rather than using the shared `Export-SolutionEvidencePackage` function. As a result, the evidence package does not pass through `Test-Copilot...
  - *Verification:* The script bypasses the shared packaging helper and therefore does not run the shared schema/hash/artifact validation path. The inline structure may be close, but drift would not be caught at runtime.
- **F-04 [LOW] VERIFIED-DOC-OVERCLAIM** — `docs/prerequisites.md` lists `scripts/common/EvidenceExport.psm1` as a shared dependency. However, Export-Evidence.ps1 does not import this module. It only imports `IntegrationConfig.psm1` (line 58)....
  - *Verification:* The documented shared dependency does not match actual script usage. This is a documentation/code mismatch unless the script is updated to use the shared evidence module.

## DEBATABLE items (need user decision)

These findings raised legitimate concerns but do not have a clear right answer without user input. We propose to leave these in place pending a decision in the PR review.

### 01-copilot-readiness-scanner — 2 debatable

- **Finding #5 [LOW]** — String literals use Windows-style backslashes (e.g., `'scripts\common'`, `'config\default-config.json'`). While `Join-Path` handles cross-platform normalization, the raw strings would not be valid pat...
  - *Why deferred:* The embedded backslashes are real and are fine on the current Windows target. If cross-platform PowerShell execution is intended, splitting path segments would be safer; recommend cleanup only if non-...
- **Finding #7 [INFO]** — The pattern `[bool]$Configuration.includeGuestAccounts` is used to evaluate boolean config values. When JSON is parsed with `-AsHashtable` and `ConvertFrom-Json`, boolean values are correctly represen...
  - *Why deferred:* The casts are real and redundant when JSON booleans are parsed correctly. They are also harmless and provide resilience if a value is supplied as a string, so no change is necessary.

### 02-oversharing-risk-assessment — 4 debatable

- **2 [LOW]** — Controls are emitted in a different order (`1.2, 1.3, 1.4, 2.5, 2.12, 1.6`) than the config files list them (`1.2, 1.3, 1.4, 1.6, 2.5, 2.12`). Should match config ordering for consistency.
  - *Why deferred:* The reviewer correctly observed that evidence output order differs from configs/tests, where 1.6 appears before 2.5. I found no functional requirement that order must match, but aligning it would redu...
- **3 [LOW]** — `DetectedSignals` is converted from array to comma-separated string via `-join ','`. This is consumed correctly downstream, but a brief comment explaining the type change would help maintainability.
  - *Why deferred:* The array-to-string conversion is real, and Export-Evidence later emits that property as-is. A comment could help maintainers, but absence of a comment is not a verified defect.
- **4 [INFO]** — The remediation mode case conversion (`Substring(0,1).ToUpper() + Substring(1)`) would throw if the string were empty. In practice, the config always supplies a non-empty value, but a guard clause or ...
  - *Why deferred:* The unguarded Substring call would throw for an empty remediationMode. All shipped configs define non-empty values, so this is robustness hardening rather than a verified runtime bug.
- **5 [INFO]** — The `ORA_ASSUME_SAM_LICENSE` environment variable pattern is a useful testing hook. A brief inline comment noting it is for CI/testing only would improve clarity for new contributors.
  - *Why deferred:* The testing hook exists and the emitted notes identify it as stub validation. An inline CI/testing-only comment may improve clarity, but the current code is not wrong.

### 03-sensitivity-label-auditor — 4 debatable

- **5 [LOW]** — Monitor-Compliance invocation does not propagate `-WhatIf`/`-Confirm` preference. Running Export-Evidence with `-WhatIf` still triggers a full monitoring pass.
  - *Why deferred:* The nested monitor script is invoked even during an export WhatIf preview and no explicit `-WhatIf`/`-Confirm` argument is passed. However, ShouldProcess preference variables normally flow to child sc...
- **12 [INFO]** — `Read-JsonFile`, `Get-SolutionRoot`, and `Get-ResolvedConfiguration` are duplicated across all three scripts.
  - *Why deferred:* The helper duplication is real. It may be acceptable for small, self-contained scaffold scripts, but a shared module would reduce drift if these scripts grow.
- **13 [INFO]** — `Get-ResolvedConfiguration` returns fewer properties than the same-named function in other scripts. Intentional but undocumented.
  - *Why deferred:* The export helper intentionally returns only the properties the exporter uses, but sharing the same function name as fuller helpers can confuse future maintainers. This is a documentation/naming clari...
- **14 [INFO]** — `evidenceRetentionDays: 2555` represents 7x365 but does not account for leap days. Conventional and acceptable, but technically 1-2 days short over a 7-year span.
  - *Why deferred:* The value is exactly 7*365 and can be short by leap days for strict calendar-year retention. This is a common retention-days convention, but should be clarified if examiners require exact seven-calend...

### 04-finra-supervision-workflow — 4 debatable

- **HIGH-001 [HIGH]** — Retry-After header parsing may not work consistently across all PowerShell 7 HttpResponseException variants. The fallback is present but the primary path needs live testing against actual 429 response...
  - *Why deferred:* The cited code really uses Headers.GetValues('Retry-After') and falls back to exponential delay. The reviewer only asserts possible PowerShell variant behavior that cannot be confirmed from the file w...
- **MEDIUM-002 [MEDIUM]** — OData filter date values are not wrapped in single quotes. While valid per OData v4 spec, some Dataverse implementations may require quoted datetime values. Should be tested.
  - *Why deferred:* The date literals are unquoted as claimed, but the finding itself concedes this is valid OData v4 syntax. Without Dataverse test evidence, requiring quotes is not clearly correct and may be harmful fo...
- **MEDIUM-005 [MEDIUM]** — Status enum values differ: evidence schema uses `implemented/partial/not-implemented`, monitor uses `monitor-only/partial`. Vocabulary inconsistency between components.
  - *Why deferred:* The vocabulary difference is real, but Monitor-Compliance output is not the same contract as the evidence package, and the shared repository schema allows monitor-only. This should be aligned or docum...
- **MEDIUM-006 [MEDIUM]** — No documented flow or procedure for reviewer re-assignment or transfer of queue items when a reviewer is unavailable. This is an operational gap for production supervision.
  - *Why deferred:* The reviewer is not fully correct that reassignment is undocumented, but the README only gives an ownership-model note and no operational transfer procedure. This is a reasonable production-readiness ...

### 05-dlp-policy-governance — 5 debatable

- **1 [LOW]** — The deployment guide does not mention the `-WhatIf` flag that `Deploy-Solution.ps1` supports (declared via `SupportsShouldProcess`). Adding a note about dry-run capability would be helpful for operato...
  - *Why deferred:* The deployment guide command omits -WhatIf, while the script help and CmdletBinding confirm WhatIf support. This is a useful documentation enhancement, not a clear defect or overclaim.
- **2 [LOW]** — The Graph connection stub requests scope `InformationProtectionPolicy.Read`. While this scope is currently valid, Microsoft is transitioning to `InformationProtectionPolicy.Read.All` in newer Graph AP...
  - *Why deferred:* The cited stub does request InformationProtectionPolicy.Read. The claim that Microsoft is transitioning to InformationProtectionPolicy.Read.All is external and unverified here, so this is only a forwa...
- **6 [LOW]** — The data flow diagram shows the evidence package filename as `05-dlp-policy-governance-evidence.json` which is correct, but the evidence export doc (`docs/evidence-export.md`, line 56) lists the SHA-2...
  - *Why deferred:* The deployment guide generically states that every JSON artifact has a SHA-256 companion, and evidence-export.md explicitly includes the package JSON. Listing the package .sha256 separately would impr...
- **3 [INFO]** — The Pester test file validates file existence, JSON structure, and syntax but does not test the Common-Functions.ps1 functions (e.g., `Get-PolicyModeValue` fallback logic, `ConvertTo-Array` edge cases...
  - *Why deferred:* The tests cover required files, config fields, syntax, and simple content checks, while Common-Functions.ps1 defines the cited helpers. Missing unit tests is a reasonable coverage improvement, not evi...
- **4 [INFO]** — The baseline snapshot construction logic is duplicated between Deploy-Solution.ps1 and Export-Evidence.ps1. The `New-BaselineSnapshot` function in Export-Evidence.ps1 could be moved to Common-Function...
  - *Why deferred:* Both scripts build very similar baseline snapshots with different baselineSource and notes. This is real duplication, but it is a refactoring opportunity rather than a verified bug.

### 06-audit-trail-manager — 5 debatable

- **F-01 [LOW]** — `framework_ids` contains `sox-302-404` which implies SOX 302 coverage, but `regulations` only lists `SOX 404`. Either `framework_ids` should be `sox-404` or `regulations` should explicitly include SOX...
  - *Why deferred:* The config matches the observation, but repository framework metadata uses id sox-302-404 with display name SOX 404 and title SOX Sections 302 and 404. It is reasonable to clarify this, but not clearl...
- **F-04 [LOW]** — The `$regulationNames` ordered hashtable is duplicated across all three scripts. Consolidating it into `AtmShared.psm1` would reduce maintenance risk and prevent future drift.
  - *Why deferred:* The hashtable is duplicated in Deploy, Export, and Monitor, while AtmShared.psm1 does not expose it. This is a reasonable maintainability improvement, but no actual drift or wrong behavior is present.
- **F-10 [LOW]** — The Pester tests do not validate the evidence test artifacts in `artifacts/evidence-test/`. Adding tests for SHA-256 integrity verification and JSON schema assertions on the evidence files would stren...
  - *Why deferred:* The tests do not reference artifacts/evidence-test, SHA-256 files, or schema validation. Adding those tests would strengthen a P0 solution, but the absence is a coverage enhancement rather than a clea...
- **F-08 [INFO]** — The architecture document does not mention the `AtmShared.psm1` module in the Processing layer description. It references `Check-AuditLogCompleteness` and `Set-RetentionPolicy` but omits the shared ut...
  - *Why deferred:* The architecture omits AtmShared.psm1 even though the scripts import it. That could be a useful architecture detail, but the current section is not clearly intended to inventory every internal helper ...
- **F-12 [INFO]** — SEC 17a-3 is listed in `regulations` but has no corresponding entry in `defaults.retentionPeriods.byRegulation`. This is technically correct because SEC 17a-3 defines what records to keep (not how lon...
  - *Why deferred:* The observation is real: SEC 17a-3 is in regulations but not retentionPeriods.byRegulation. That is also substantively reasonable because 17a-3 defines records while 17a-4 drives retention duration, s...

### 07-conditional-access-automation — 3 debatable

- **F-02 [HIGH]** — The `Merge-Configuration` function produces different output shapes in each script. Deploy-Solution includes `version`, `regulations`, `evidenceOutputs`, `status`, and `doraAlignmentDocumented`; Monit...
  - *Why deferred:* The Merge-Configuration output shapes do differ across scripts. Some fields are script-specific, so this is a documented-maintenance-risk finding rather than a clear functional bug.
- **F-01 [MEDIUM]** — Five utility functions (`Read-JsonFile`, `Write-JsonFile`, `Resolve-ConfiguredPath`, `Merge-Configuration`, `New-PolicyTemplate`) are duplicated across all three scripts. While this is acknowledged in...
  - *Why deferred:* Duplication is real and explicitly documented as tech debt, but the finding overstates that five helpers are duplicated across all three scripts because Export-Evidence does not define Write-JsonFile....
- **F-03 [MEDIUM]** — The `Merge-Configuration` in `Export-Evidence.ps1` does not include `$DefaultConfig.version` in its output, unlike `Deploy-Solution.ps1`. While the version is not currently consumed by the export pipe...
  - *Why deferred:* Export-Evidence omits version while Deploy-Solution includes it. The finding itself notes the value is not consumed, so this is a metadata improvement rather than a verified bug.

### 08-license-governance-roi — 4 debatable

- **1 [MEDIUM]** — Version field shows `v0.1.0` but CHANGELOG documents v0.2.0 with consumption billing features. The version should be updated to `v0.2.0` to reflect the consumption billing additions.
  - *Why deferred:* The cited config line is v0.1.1, not v0.1.0. An undated v0.2.0 section exists, but the dated top changelog entry is v0.1.1, so the current-release claim is ambiguous rather than cleanly verified.
- **2 [MEDIUM]** — README header shows `Version: v0.1.0` but should match the current release documented in CHANGELOG (v0.2.0).
  - *Why deferred:* The README does not show v0.1.0 as claimed; it shows v0.1.1. The v0.2.0 section makes release state unclear, but the top dated changelog entry matches the README.
- **3 [LOW]** — The default value `[int]$InactivityThresholdDays = 30` is misleading because the parameter is overridden by tier configuration when not explicitly bound. Consider changing the default to `0` or removi...
  - *Why deferred:* The default value exists but is bypassed unless the parameter is explicitly bound, so the concern is real as a readability issue. Runtime behavior still correctly prefers tier configuration, making th...
- **7 [LOW]** — Test coverage is minimal -- 8 tests covering file presence, help, parameters, and basic config checks. No functional tests validate the config merge logic, evidence output structure, or consumption bi...
  - *Why deferred:* The test file contains eight smoke/config checks and no functional merge or evidence-structure assertions. That is valid scaffolding coverage, but whether it is insufficient is a quality judgment.

### 09-feature-management-controller — 6 debatable

- **8 [MEDIUM]** — SHA-256 hash comparison relies on PowerShell's case-insensitive string comparison rather than explicit normalization. Works correctly but could be more robust.
  - *Why deferred:* The comparison does rely on PowerShell's default case-insensitive -ne behavior, so the observation is accurate. Because SHA-256 hex case differences still compare equal, this is robustness/style rathe...
- **11 [LOW]** — Redundant `@()` wrapping of already-typed `[object[]]` parameter.
  - *Why deferred:* The redundant @() wrapper is present, but it is harmless and can protect count semantics if callers pass unusual values. This is not a functional defect.
- **12 [LOW]** — AST-based function extraction may miss functions that depend on script-level variables.
  - *Why deferred:* The test harness extracts only function definitions, so the general concern is plausible. I did not verify a current function that requires script-level state, so this remains a test-design caveat rat...
- **20 [LOW]** — PowerShell 7+ requirement could be more specific about minimum version 7.0 and the reason (inline conditional expressions, `-AsHashtable`).
  - *Why deferred:* The prerequisite already states PowerShell 7 or later, which is effectively a 7.0+ minimum. Adding the reason could help operators, but the cited requirement is not missing.
- **3 [LOW]** — Array concatenation (`+=`) in a loop is O(n^2). Minor for documentation-first but still an anti-pattern.
  - *Why deferred:* The += pattern is present and can be inefficient for large loops. In this documentation-first scaffold the feature list is small, so this is a maintainability/performance nit rather than a verified fu...
- **5 [LOW]** — Indentation inconsistency: `try` block body is not indented relative to the `try` keyword.
  - *Why deferred:* The indentation inconsistency is real. It is a style/readability issue only; PowerShell execution is unaffected.

### 10-connector-plugin-governance — 7 debatable

- **12 [LOW]** — Duplicate `Get-ConnectorGovernanceConfiguration` function definition. Should be in shared module.
  - *Why deferred:* The duplicate helper is verified, but it is a maintainability concern rather than a runtime bug. Moving it to a shared module would be reasonable but not required to correct behavior.
- **15 [LOW]** — No Pester test for comment-based help in Export-Evidence.ps1 or Monitor-Compliance.ps1.
  - *Why deferred:* The test gap is real: only Deploy-Solution.ps1 help is asserted. This is coverage improvement rather than a verified solution bug because the scripts themselves already contain comment-based help.
- **16 [LOW]** — No test for cross-tier config property consistency (e.g., all tiers should define `approvalModel` and `approvalSLAHours`).
  - *Why deferred:* The tests do not include a cross-tier property consistency check. That is a useful guardrail for future edits but not itself a verified runtime bug.
- **17 [LOW]** — `AlertOnNewConnectors` parameter test does not validate type is `[switch]`.
  - *Why deferred:* The test only checks parameter presence and does not assert the switch type, while the script currently declares it correctly. This is a test-strengthening suggestion, not a code defect.
- **21 [LOW]** — Architecture diagram arrow from Solution 09 to approval router could be misread as a runtime data dependency.
  - *Why deferred:* The diagram arrow can be read as a dependency, but surrounding deployment text frames Solution 09 as rollout gating rather than a live runtime feed. This is a clarity issue, not a clear overclaim.
- **23 [LOW]** — Declarative agents are mentioned in the overview but no agent-specific governance logic exists in scripts or config. Scope boundary correctly excludes this but the introduction creates undelivered exp...
  - *Why deferred:* The overview does create some expectation around declarative agents, but the README limitations and architecture keep agent/plugin inventory largely documentation-first and manually reconciled. Tighte...
- **8 [LOW]** — `$config.approvalSLAHours[$connector.riskLevel]` lacks null safety for unexpected risk level values.
  - *Why deferred:* The lookup has no fallback, but the script's hardcoded sample inventory only uses risk levels present in config. It becomes a bug only if this scaffold later consumes external or edited risk values.

### 11-risk-tiered-rollout — 3 debatable

- **F-01 [MEDIUM]** — Graph API permission reference for `POST /users/{id}/assignLicense` should be updated to note deprecation and recommend group-based licensing, which aligns with the `groupMode: wave-based` configurati...
  - *Why deferred:* The repo confirms a direct user assignLicense reference and a wave-based group mode, but API deprecation is an external claim not verifiable from local code. The README scope also says scripts do not ...
- **F-15 [INFO]** — Deployment guide does not specify which waves are available per governance tier (baseline = 0-1, recommended = 0-2, regulated = 0-3).
  - *Why deferred:* The deployment guide itself omits a tier-to-wave table, but architecture.md already documents the same tier boundaries. Whether this must be duplicated in the deployment guide is a documentation judgm...
- **F-19 [INFO]** — Control 1.12 (Training and Awareness) claim of "partial" status is generous given the thin training evidence. "Monitor-only" would be more accurate.
  - *Why deferred:* The finding is partly true because recommended and regulated statuses are partial, but baseline is already monitor-only and the repository labels coverage as scaffold evidence-export-ready. Whether pa...

### 12-regulatory-compliance-dashboard — 6 debatable

- **Finding 1: Indentation Inconsistency in  [LOW]** — The first element of the fallback control array in `Get-ControlStatusSnapshot` is indented with extra whitespace (8 spaces + 4 spaces for properties) compared to the remaining 5 elements (8 spaces + 4...
  - *Why deferred:* The indentation inconsistency is real: the return statement and first object are indented deeper than the remaining fallback objects. PowerShell indentation is not semantic, so this is cosmetic rather...
- **Finding 4: Monitor-Compliance.ps1 Freshn [LOW]** — The `FreshnessThresholdHours` parameter has a default of `25`, which corresponds to the recommended/regulated tier threshold. However, the `ConfigurationTier` parameter defaults to `baseline`, which h...
  - *Why deferred:* The mismatch is real: the parameter default is 25 while the baseline tier config is 48. The script overrides the parameter default from tier config when the caller did not bind it, so behavior is corr...
- **Finding 7: Pester Tests Do Not Exercise  [LOW]** — The test file validates Deploy-Solution.ps1 syntax parsing and checks for comment-based help, but does not execute the script (even with `-WhatIf`). Monitor-Compliance.ps1 and Export-Evidence.ps1 are ...
  - *Why deferred:* The test file only parses Deploy-Solution.ps1, while Monitor-Compliance.ps1 and Export-Evidence.ps1 are executed. This is a coverage gap at most; it is not evidence of broken product behavior.
- **Finding 9: Evidence Export Dashboard Rep [LOW]** — Export-Evidence.ps1 hardcodes `reportName = 'FSI Copilot Governance Dashboard'` (line 189), while default-config.json specifies `powerBI.templateName = 'RCD-GovernanceDashboard.pbit'` and the architec...
  - *Why deferred:* The report display name differs from the template filename and architecture shorthand. That may be intentional naming rather than a defect, but the inconsistency is real and could confuse downstream c...
- **Finding 10: Evidence Schema Status Enum  [INFO]** — The evidence schema defines allowed control status values as `["implemented", "partial", "monitor-only", "playbook-only"]`. The `IntegrationConfig.psm1` module also defines a score for `"not-applicabl...
  - *Why deferred:* The indentation inconsistency is real: the return statement and first object are indented deeper than the remaining fallback objects. PowerShell indentation is not semantic, so this is cosmetic rather...
- **Finding 8: DELIVERY-CHECKLIST.md Pre-Del [INFO]** — The validation commands use forward slashes (`pwsh scripts/Deploy-Solution.ps1`) while the Pester command uses backslashes (`Invoke-Pester tests\12-regulatory-compliance-dashboard.Tests.ps1`). PowerSh...
  - *Why deferred:* The checklist mixes forward slashes and backslashes exactly as reported. The commands are still valid in PowerShell, so this is cosmetic rather than a bug.

### 13-dora-resilience-monitor — 2 debatable

- **F-06 [LOW]** — Line 97: `$configuration.Contains('evidenceImmutability')` always returns `$true` because `Get-DrmConfiguration` always sets this key (to the value or `$null`). The effective guard is `$null -ne $conf...
  - *Why deferred:* Get-DrmConfiguration always returns an evidenceImmutability key, so the Contains check is redundant and the null check controls behavior. This is a harmless simplification opportunity rather than a fu...
- **F-13 [INFO]** — No unit tests for `Invoke-DoraSeverityClassification` threshold logic. Adding boundary-condition tests would improve confidence in the classification model, especially for the minor-to-significant esc...
  - *Why deferred:* The tests cover Graph status normalization paths, not boundary cases for the severity threshold function. This is a useful coverage improvement but not itself evidence of a runtime defect.

### 14-communication-compliance-config — 5 debatable

- **M-01 [MEDIUM]** — Duplicate function name `New-PolicyCatalog` in two different scripts with different signatures and return types. Creates maintenance confusion.
  - *Why deferred:* The duplicate function name and different signatures are real. However, they are local helper functions in separate scripts, so this is a maintainability concern rather than a verified runtime defect.
- **M-03 [MEDIUM]** — Magic number `+ 2` in `recordCount` calculation. Should be documented or extracted to a named variable.
  - *Why deferred:* The `+ 2` literal is present and undocumented. It likely represents the two non-template artifacts, so this is a clarity issue rather than a verified functional bug.
- **L-01 [LOW]** — `Get-CopilotGovTierDefinition` call could benefit from an inline comment explaining its purpose in context.
  - *Why deferred:* The cited call has no inline comment, but its purpose is reasonably inferable from the subsequent TierLabel/TierValue output. This is a readability preference, not a defect.
- **L-04 [LOW]** — `.PARAMETER` check excludes `.psm1` files but the CCC-Common module functions lack function-level `.PARAMETER` documentation. Functions only have module-level help.
  - *Why deferred:* The test deliberately excludes .psm1 files from the .PARAMETER assertion, and CCC-Common functions lack function-level help. Whether public module helpers must have per-function help is a documentatio...
- **L-06 [LOW]** — "v1.3+ content preview in IRM alerts" references an unexplained version number. Should specify the product name.
  - *Why deferred:* The unexplained `v1.3+` reference is present and could confuse readers. This is a documentation clarity issue rather than an overclaim or functional defect.

### 15-pages-notebooks-gap-monitor — 2 debatable

- **F-03 [LOW]** — - Files: - `Deploy-Solution.ps1` lines 75-146 (5 gaps) - `Monitor-Compliance.ps1` lines 96-168 (5 gaps across two functions) - `Export-Evidence.ps1` lines 105-156 (5 gaps) - Impact: Gap metadata (desc...
  - *Why deferred:* The duplication exists, but AGENTS.md says scripts deliberately use representative sample data in this documentation-first scaffold. This is a maintainability concern rather than a confirmed runtime d...
- **F-05 [LOW]** — "Notebooks (OneNote in Teams/SharePoint)" is the only reference to OneNote in the entire solution. All other documentation uses "Notebooks" without the OneNote qualifier.
  - *Why deferred:* Search confirms this is the only `OneNote` reference in the solution, but it appears in the historical v0.1.0 changelog rather than current operational documentation. Treating historical wording as a ...

### 16-item-level-oversharing-scanner — 3 debatable

- **F2 [HIGH]** — ConvertTo-Hashtable, Merge-Hashtable, and Get-Configuration functions are duplicated across 3-4 scripts (~80 lines each). Should be extracted to a shared module.
  - *Why deferred:* The duplication is real and also appears in Deploy-Solution.ps1, but it is a maintainability refactor rather than a functional defect proven by the cited code. Severity and mandatory extraction are ju...
- **F5 [MEDIUM]** — The `$sensitiveLabels` array is hardcoded in the script and not loaded from configuration, creating maintenance divergence with risk-thresholds.json.
  - *Why deferred:* The hardcoded sensitive-label keyword list is real, but risk-thresholds.json does not currently define an equivalent label list, so the claimed divergence is partly a design/configuration gap rather t...
- **F16 [LOW]** — Content-type keywords "trade", "customer", and "legal" are too broad and will match unintended items in production environments.
  - *Why deferred:* The keywords are broad and the matching surface is broad, but whether they will cause unintended production matches is environment-dependent. F4 verifies a concrete false-positive path for legal; the ...

### 17-sharepoint-permissions-drift — 6 debatable

- **9 [MEDIUM]** — `baseline-config.json` and `baseline.json` have conflicting values for `maxSitesPerRun` (500 vs 100), `scanFrequencyHours` (24 vs 48), and `baselineRetentionDays` (90 vs 60).
  - *Why deferred:* The values differ, but they appear to be different configuration layers rather than a pure conflict. scanFrequency is tier-overridden, while maxSites/retention are not, so this is mostly a precedence/...
- **16 [LOW]** — Windows backslash path separators in module import strings limit cross-platform portability.
  - *Why deferred:* Backslash separators are present and may reduce PowerShell Core portability, but this task/repo execution context is Windows-first and no concrete failure is shown. This is a low-severity portability ...
- **17 [LOW]** — Array concatenation pattern (`$array += $item`) in loops has O(n^2) performance.
  - *Why deferred:* The pattern exists, but scaffold/sample data sizes are small and no performance target is violated. It becomes relevant only for large live tenant scans.
- **18 [LOW]** — `Connect-PnPOnline` called without any authentication parameters.
  - *Why deferred:* The live placeholder does not expose an authentication mode, but it may rely on a pre-established PnP context and is not a demonstrated scaffold failure. For live binding, authentication should be exp...
- **21 [LOW]** — Evidence period hardcoded to 30 days instead of deriving from actual data range.
  - *Why deferred:* The hard-coded 30-day window is real, but docs do not state the period is derived from data; it may be an intended default reporting window. Treat this as a quality issue rather than a verified bug.
- **25 [LOW]** — No unit tests for core logic functions (`Compare-PermissionSets`, `Get-DriftTypeWeight`, `Get-RiskTier`).
  - *Why deferred:* There are no Pester assertions exercising Compare-PermissionSets, Get-DriftTypeWeight, or Get-RiskTier. This is a test gap, not a product behavior bug.

### 18-entra-access-reviews — 3 debatable

- **6 [LOW]** — `ConvertTo-Hashtable` and `Merge-Hashtable` duplicated in 3 scripts (Deploy, Export, Monitor). Should be in a shared module.
  - *Why deferred:* The helper duplication is real across Deploy, Export, and Monitor. This is a maintainability concern rather than an independently verifiable runtime bug.
- **15 [INFO]** — Module import paths use Windows backslash separators. PowerShell Core on Linux/macOS would not resolve these paths correctly.
  - *Why deferred:* The scripts do use Windows-style path segments in module imports. The impact is cross-platform compatibility, but no explicit non-Windows support requirement was found, so this is a portability concer...
- **17 [INFO]** — No functional/unit tests for individual functions. Tests cover structure, config, and syntax only.
  - *Why deferred:* The Pester file covers structure, configuration, dependency declaration, evidence docs, and syntax, not function behavior. That is a test coverage gap, but absence of unit tests is not itself a runtim...

### 19-copilot-tuning-governance — 4 debatable

- **F01 [MEDIUM]** — `ConvertTo-Hashtable` and `Merge-Hashtable` duplicated from other scripts; should be extracted to shared module
  - *Why deferred:* The helper functions are present in this script and similar helpers appear in the other scripts, so the duplication exists. Extracting them is a maintainability preference rather than a verified runti...
- **F11 [LOW]** — `evidenceRetentionDays: 2555` derivation (7 years) not documented in configuration or deployment guide
  - *Why deferred:* The 2555-day value is documented in deployment guidance and evidence-export docs, but the seven-year derivation is not explicitly stated there. This is a clarity improvement, not a verified functional...
- **F14 [LOW]** — Missing troubleshooting entry for shared module import failures (`EvidenceExport.psm1`, `IntegrationConfig.psm1`)
  - *Why deferred:* Troubleshooting already mentions EvidenceExport.psm1, but it does not explicitly cover Import-Module failures or IntegrationConfig.psm1. This is a support-doc polish item rather than a verified defect...
- **F15 [LOW]** — Missing tests for `requireExaminerReadyEvidence` tier progression, `maxTuningRequestsPerCycle` values, and `approvalChain` growth
  - *Why deferred:* The tests cover several tier differences, but grep found no tests for requireExaminerReadyEvidence, maxTuningRequestsPerCycle, or approvalChain. This is a test coverage enhancement, not evidence of br...

### 20-generative-ai-model-governance-monitor — 3 debatable

- **F-14 [MEDIUM]** — EU AI Act (Regulation (EU) 2024/1689) is not referenced despite the solution's comprehensive regulatory stance; relevant for FSI clients with European operations, particularly given Annex III high-ris...
  - *Why deferred:* The reviewed regulatory list does not include the EU AI Act, and repository search found no EU AI Act references for this solution. No repository requirement or cited scope makes EU AI Act coverage ma...
- **F-03 [LOW]** — Comment-based help documents `.PARAMETER WhatIf` but the parameter is implicitly provided by `SupportsShouldProcess`; technically correct but slightly misleading
  - *Why deferred:* The script does not declare a WhatIf parameter, but SupportsShouldProcess supplies the common WhatIf parameter. Whether documenting that common parameter in comment help is misleading is subjective.
- **F-11 [LOW]** — `monitoring_log_retention_days` of 2555 (7 years) is reasonable but should be documented as a configurable default aligned with financial services retention norms
  - *Why deferred:* The 2555-day regulated retention default is present, and README points to repository data-classification metadata for retention. Whether this solution also needs an explicit seven-year rationale is a ...

### 21-cross-tenant-agent-federation-auditor — 1 debatable

- **10 [INFO]** — No execution-level tests for any of the three main scripts. Expanding tests to run scripts in sample mode and validate output structure would strengthen the scaffold.
  - *Why deferred:* Verified as a coverage gap: the tests cover presence, config shape, parse checks, and module loading, but do not execute Deploy, Monitor, or Export scripts. This strengthens quality but is not itself ...

### 22-pages-notebooks-retention-tracker — 2 debatable

- **F-03 [HIGH]** — The lineage hash is generated as `"sha256:sample-{0:x16}"` which produces strings like `"sha256:sample-0000000012d687"`. A real SHA-256 hash is 64 hex characters; this format is only 14-16 hex charact...
  - *Why deferred:* Line 150 emits a nonstandard sample lineage token when signed lineage is enabled. Empirical Export-Evidence check showed the generated artifact companion hash was valid: recorded=770bccbe28bae477ec69e...
- **F-14 [LOW]** — The script unconditionally outputs `$deploymentManifest` to the pipeline (line 134) without a `-PassThru` switch. Monitor-Compliance.ps1 and Export-Evidence.ps1 both use the `-PassThru` pattern. This ...
  - *Why deferred:* Deploy-Solution does always emit the manifest object and has no PassThru switch. That is inconsistent with the other scripts, but the script's stated purpose is to produce a manifest for review, so wh...

### 23-copilot-studio-lifecycle-tracker — 2 debatable

- **F-03 [LOW]** — Export-Evidence.ps1 defines its own `Write-Sha256Companion` function (lines 60-71) that duplicates the logic in the shared `Write-CopilotGovSha256File` function from `EvidenceExport.psm1`. Both functi...
  - *Why deferred:* The duplicate hash helper is real, but the output format is functionally equivalent and no standalone failure is shown. The stronger issue is the shared packaging/validation bypass captured in F-02.
- **F-05 [LOW]** — The evidence package metadata sets `version = $configuration.version` (which is `"v0.1.0"` from default-config.json). The shared evidence schema does not require a `version` field in metadata (only `e...
  - *Why deferred:* The extra version field is present, but the schema explicitly allows additional metadata and the current config is v0.1.1, not the finding's v0.1.0. The naming could be clarified, but no current schem...

## REJECTED-WRONG items (reviewer was wrong — no action)

These items were classified after empirical check showed the council finding was stale, mistaken, or based on outdated state. No repo change required.

### 01-copilot-readiness-scanner — 3 rejected

- **Finding #1 [MEDIUM]** — CHANGELOG.md has an entry for `v0.3.0` that adds the Admin Center reference, but all other version references remain at `v0.2.0`. The v0.3.0 entry also does not...
  - *Why rejected:* The current version entry is v0.2.1 with brackets and a date, not v0.3.0. README, default config, and delivery checklist also show v0.2.1, so the alleged drift is not present.
- **Finding #2 [MEDIUM]** — Two tests fail because the solution scripts call shared module functions (`New-CopilotGovGraphContext` from GraphAuth.psm1, `Test-CopilotGovEvidencePackage` fro...
  - *Why rejected:* The cited testResults.xml shows failures, but current shared modules define and export the missing functions; EvidenceExport.psm1 likewise exports Test-CopilotGovEvidencePackage. CRS-Common resolves t...
- **Finding #4 [LOW]** — The v0.3.0 entry uses the format `## v0.3.0` without brackets or a date, while v0.2.0 and v0.1.0 use `## [v0.2.0] - 2026-03-07` format.
  - *Why rejected:* The current changelog heading already follows the bracketed version plus date format. There is no `## v0.3.0` heading at the cited line.

### 03-sensitivity-label-auditor — 5 rejected

- **1 [MEDIUM]** — `InformationProtectionPolicy.Read` is not a standard Microsoft Graph permission scope. The correct permission is likely `InformationProtectionPolicy.Read.All`.
  - *Why rejected:* The cited row already distinguishes delegated from application permissions; the application column uses `InformationProtectionPolicy.Read.All`. The reviewer premise that the row only uses the non-All ...
- **4 [LOW]** — `New-Item` directory creation occurs outside the `ShouldProcess` guard, so `-WhatIf` will still create the output directory.
  - *Why rejected:* The directory creation is not inside an explicit `$PSCmdlet.ShouldProcess` block, but `New-Item` is itself ShouldProcess-aware and honors `-WhatIf` through `$WhatIfPreference`. The specific claim that...
- **8 [LOW]** — Inherits the same `InformationProtectionPolicy.Read` permission name issue from prerequisites.md.
  - *Why rejected:* Troubleshooting already separates delegated and application permissions and uses `InformationProtectionPolicy.Read.All` for app-only organization enumeration. This repeats the rejected premise from fi...
- **9 [LOW]** — `prioritySites` is empty with no example or placeholder to guide operators.
  - *Why rejected:* `prioritySites` is empty in the default config, but the deployment guide explicitly tells operators how to populate it. The claim of no guidance is false.
- **11 [INFO]** — The label groups migration timeline (December 2025 through May 2026) may need a freshness update since this window should now be complete or nearly complete.
  - *Why rejected:* The cited README no longer contains a December 2025-May 2026 timeline. The freshness concern appears to be based on stale text.

### 04-finra-supervision-workflow — 1 rejected

- **LOW-001 [LOW]** — Action naming pattern regex does not match all documented flow names. Cosmetic inconsistency.
  - *Why rejected:* The cited action names all start with uppercase letters and contain only letters, numbers, and spaces, so they match the regex. I found no documented flow name in the cited file that violates the patt...

### 07-conditional-access-automation — 1 rejected

- **F-05 [MEDIUM]** — Line 211 reads `$Configuration.namedLocations.Count` without wrapping in `@()`. In `Export-Evidence.ps1` (line 194) and `Monitor-Compliance.ps1` (line 246), the...
  - *Why rejected:* The cited line no longer reads `$Configuration.namedLocations.Count`, and the current matching named-location code wraps values in `@()`. This appears to be a stale or incorrect citation.

### 08-license-governance-roi — 2 rejected

- **6 [LOW]** — The status determination logic has a subtle precedence issue: if there are zero flagged seats AND utilization is at/above threshold, the status is `implemented`...
  - *Why rejected:* Inactive seats always hit the partial branch before Viva status is considered, so the README-based claim about inactive seats becoming monitor-only is not supported by code. The zero-flagged/Viva-off ...
- **10 [INFO]** — The Copilot usage report endpoints (`getMicrosoft365CopilotUsageUserDetail` and `getMicrosoft365CopilotUsageUserCounts`) are in the `/beta` namespace. These end...
  - *Why rejected:* The cited architecture lines use /v1.0, not /beta. The finding's beta-namespace assertion is stale or wrong.

### 09-feature-management-controller — 4 rejected

- **18 [HIGH]** — Version mismatch: CHANGELOG shows v0.2.0 but README, DELIVERY-CHECKLIST, and default-config.json show v0.1.0.
  - *Why rejected:* I verified the version-bearing files named in the finding, and they all show v0.1.1 rather than the alleged v0.2.0/v0.1.0 split. The version-drift claim is stale/wrong.
- **1 [LOW]** — Inline `if` expression in ordered hashtable construction requires PS7+ -- should document this requirement.
  - *Why rejected:* The cited script does use inline if assignments in an ordered hashtable, but the solution prerequisites already document PowerShell 7 or later. The claim that this requirement should be documented is ...
- **23 [LOW]** — Graph `featureRolloutPolicies` endpoint described as the source for Copilot feature rollout policy state, but this endpoint is specifically for Azure AD staged ...
  - *Why rejected:* The cited architecture section does not describe Microsoft Graph featureRolloutPolicies as the Copilot rollout source. It instead lists admin center and Cloud Policy surfaces, so this finding is stale...
- **16 [INFO]** — Graph `featureRolloutPolicies` endpoint is for Azure AD feature rollout, not specifically Copilot features. Not technically wrong but potentially misleading.
  - *Why rejected:* The cited default config no longer points to featureRolloutPolicies; it explicitly sets the Graph endpoint to null and says no Graph Copilot feature-rollout endpoint is configured. This finding is sta...

### 10-connector-plugin-governance — 5 rejected

- **4 [MEDIUM]** — Accessing `$TierConfig.approvalModel.autoApproveMicrosoftBuilt` under StrictMode when baseline.json is the active tier config. This property IS present in basel...
  - *Why rejected:* The property is only read in the baseline switch branch, and baseline.json defines it. The suggested StrictMode failure is hypothetical and not reachable in the cited code path.
- **18 [LOW]** — `autoApproveMicrosoftBuilt` is defined in baseline.json but not in recommended.json or regulated.json. Access under StrictMode from non-baseline branches would ...
  - *Why rejected:* The cross-tier absence is real, but the code does not access autoApproveMicrosoftBuilt from recommended or regulated branches. StrictMode failure from non-baseline branches is not verified.
- **2 [LOW]** — `ConvertFrom-Json -Depth 20` requires PowerShell 7.3+. Prerequisites doc specifies only "PowerShell 7".
  - *Why rejected:* The repo evidence shows use of -Depth and a PowerShell 7 prerequisite, but does not substantiate the asserted PowerShell 7.3+ minimum. This should not be verified as version drift from the cited code ...
- **22 [LOW]** — Should specify "PowerShell 7.3+" instead of "PowerShell 7" given script dependencies on `-Depth` parameter.
  - *Why rejected:* This repeats the unsubstantiated 7.3+ assertion from finding 2. The cited files show a PowerShell 7 prerequisite and -Depth usage, not proof that 7.3+ is required.
- **9 [LOW]** — `.ContainsKey()` call assumes hashtable type. Mismatch with Deploy-Solution's PSCustomObject pattern.
  - *Why rejected:* Export-Evidence.ps1 deliberately loads config as hashtables, so ContainsKey is valid in that script. The PSCustomObject pattern in Deploy-Solution.ps1 is not used by this code path.

### 11-risk-tiered-rollout — 1 rejected

- **F-04 [MEDIUM]** — `framework_ids` in default-config.json lists GLBA-501b, SOX-302-404, FFIEC-IT-Handbook, and Interagency-AI-Guidance but none of these have documentation, contro...
  - *Why rejected:* The IDs are not only orphaned in default-config; repository-level solution-to-playbooks, frameworks-master, and examination-readiness mappings also list them for solution 11. The README regulatory lis...

### 13-dora-resilience-monitor — 3 rejected

- **F-04 [MEDIUM]** — Lists `Microsoft.Graph.Reports` and `ExchangeOnlineManagement` as required PowerShell modules. Neither is used by any DRM script. The correct module for future ...
  - *Why rejected:* The current prerequisites do not list Microsoft.Graph.Reports, and ExchangeOnlineManagement is explicitly optional rather than required. The finding appears stale or based on an older file state.
- **F-05 [MEDIUM]** — Lists `SecurityEvents.Read.All` as a required API permission. This permission is for the Security API, not service health. The DRM solution only requires `Servi...
  - *Why rejected:* SecurityEvents.Read.All is not listed as required; it is marked optional and limited to intentional legacy Security API use. The required service-health permission is ServiceHealth.Read.All.
- **F-14 [INFO]** — Version v0.2.0 is listed as unreleased but the README and all configs still reference v0.1.0. The changelog entry for v0.2.0 describes the roadmap awareness sec...
  - *Why rejected:* There is no v0.2.0 unreleased entry in the current changelog, and README/default-config both reference v0.1.1. The finding is stale or based on a different revision.

### 14-communication-compliance-config — 3 rejected

- **H-01 [HIGH]** — Version field is `v0.1.0` but CHANGELOG documents `v0.2.0` with already-implemented features (IRM integration, video recap compliance artifacts documented in ar...
  - *Why rejected:* The cited config is not stale at v0.1.0; it already shows v0.2.1. The CHANGELOG latest entry is also v0.2.1, so the version-drift claim is stale.
- **I-02 [INFO]** — Version in README header does not match CHANGELOG latest version.
  - *Why rejected:* The README status line and CHANGELOG latest version match at v0.2.1. The mismatch claim is stale or wrong.
- **I-03 [INFO]** — Hardcoded date reference "March 2026" for video recap feature will become stale.
  - *Why rejected:* The cited architecture section no longer contains a hardcoded `March 2026` or `video recap` reference. It describes Intelligent recap and audio recap instead, so the finding is stale.

### 15-pages-notebooks-gap-monitor — 1 rejected

- **F-06 [INFO]** — The Known Limitations section notes that "retention policies and basic Microsoft Purview eDiscovery support are generally available for Copilot Pages and Loop c...
  - *Why rejected:* The finding claims GAP-001 and GAP-002 are all still high/open, but GAP-001 is already medium/validation-required in the cited script data. GAP-002 remains high/open because the README lists review-se...

### 17-sharepoint-permissions-drift — 1 rejected

- **12 [MEDIUM]** — Imports `IntegrationConfig.psm1` which is not imported by any other script and not validated by the test suite.
  - *Why rejected:* The import is not dead: the script uses a function exported by IntegrationConfig, and EvidenceExport also imports that shared module. Test coverage could be stronger, but the finding's implication tha...

### 18-entra-access-reviews — 1 rejected

- **9 [LOW]** — Decision status comparison uses exact-case match (`'NotReviewed'`). Graph API may return `notReviewed` (camelCase). Should use case-insensitive comparison.
  - *Why rejected:* PowerShell's -eq operator is case-insensitive by default, so notReviewed and NotReviewed both match. The cited code does not have the exact-case bug described.

### 19-copilot-tuning-governance — 2 rejected

- **F09 [LOW]** — `riskReassessmentDays: -1` sentinel value not validated in scripts; could cause arithmetic issues
  - *Why rejected:* The -1 baseline value is only copied into the deployment manifest; grep found no script arithmetic using riskReassessmentDays. The cited arithmetic-risk claim does not match the actual code.
- **F17 [INFO]** — Component diagram does not show risk assessment as a distinct governance gate
  - *Why rejected:* The diagram does include tuning risk assessment within the Tuning Approval Workflow box. It is not a separate component, but the governance gate is represented.

### 20-generative-ai-model-governance-monitor — 2 rejected

- **F-07 [MEDIUM]** — `ThirdPartyReview` is a singleton wrapped in `@()` for JSON array consistency; correct behavior but should be documented as intentional since a future multi-ven...
  - *Why rejected:* ThirdPartyReview is not hardcoded as a singleton; it creates one record per unique modelProvider. The Export-Evidence @() wrapper preserves array shape and does not prove a singleton data model.
- **F-08 [LOW]** — `recordCount` calculation hardcodes `+ 1` for the third-party review singleton instead of using `@($snapshot.ThirdPartyReview).Count`; fragile for future multi-...
  - *Why rejected:* The cited hardcoded '+ 1' is not present. recordCount already uses @($snapshot.ThirdPartyReview).Count for the third-party review records.

### 21-cross-tenant-agent-federation-auditor — 2 rejected

- **2 [MEDIUM]** — `signingKeyThumbprint` field described as "Sha-256 thumbprint" but sample data uses 40-character SHA-1 thumbprints. Document should state "SHA-1 thumbprint" to ...
  - *Why rejected:* Rejected. The cited evidence doc does not describe a `signingKeyThumbprint` field, and solution-wide search found no thumbprint field or sample value to compare.
- **7 [LOW]** — Several regulated-only fields (`requireSecondPartyApproval`, `requireExternalAttestor`, `alertOnAttestationStale`, `alertOnRotationOverdue`, `auditAggregation.*...
  - *Why rejected:* Rejected. The named nested regulated fields are carried through their parent objects and written into the deployment manifest; they are not absent from all script consumption.

### 22-pages-notebooks-retention-tracker — 2 rejected

- **F-08 [MEDIUM]** — The clone URL `https://github.com/judeper/FSI-CopilotGov-Solutions.git` references a specific user's GitHub account rather than an organizational repository.
  - *Why rejected:* The URL is indeed user-owned, but the target repository path and review context are for judeper/FSI-CopilotGov-Solutions. No organizational repository target is established, so this is not a verified ...
- **F-13 [LOW]** — The version date `2025-01-15` does not include a timezone. This is standard for changelog entries and follows Keep a Changelog convention, but the solution's ev...
  - *Why rejected:* The dates lack timezone offsets, but that is normal Keep a Changelog style for release dates. Evidence artifacts using ISO 8601 timestamps are a different context.

## Next steps

1. **User approval requested** before any PRs land. Open questions:
   - Branch convention: `accuracy-review/2026-05-22-remediation/<slug>` (mirrors prior 2026-05-04 cycle) or `council-review/2026-05-22/<slug>`?
   - File a GH issue per VERIFIED-BUG first, then PRs? Or PR directly with finding references in commit body?
   - DEBATABLE handling: comment in PR vs separate issue vs defer entirely?
2. **PR generation (after approval)** — per-solution PRs in waves of 5, isolated in worktrees at `c:\worktrees\rem-<slug>`. Each PR will:
   - Apply only VERIFIED-* fixes for that solution.
   - Bump CHANGELOG.
   - Run all repository validators before push.
3. **Final summary** at end of cycle linking every PR back to this triage report.