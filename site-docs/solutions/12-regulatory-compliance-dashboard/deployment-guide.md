# Deployment Guide

1. Review the mapped controls and confirm scope.
2. Select a governance tier.
3. Update the configuration JSON files under `../config/`.
4. Run `../scripts/Deploy-Solution.ps1` with the chosen tier.
5. Export and review evidence after any configuration change.

## Dependencies

06-audit-trail-manager, 11-risk-tiered-rollout
